/// Moroccan auto-entrepreneur activity families (IR/CNSS rates differ per category).
/// Authoritative percentages must come from versioned config / official sources — not hardcoded for production.
enum ActivityCategory {
  commercial,
  artisanal,
  liberal,
  services,
}
